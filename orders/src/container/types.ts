const types = {
    AddressService: Symbol.for('AddressService'),
    AddressController: Symbol.for('AddressController'),
    TenantConnectionManager: Symbol.for('TenantConnectionManager'),
    TenantConnection: Symbol.for('TenantConnection'),
};

export default types;